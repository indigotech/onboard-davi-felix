import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    marginTop: 32,
    marginBottom: 32,
    marginHorizontal: 8,
    flex: 1,
  },
  title: {
    color: '#090909',
    fontSize: 32,
    fontWeight: 'bold',
  },
});
