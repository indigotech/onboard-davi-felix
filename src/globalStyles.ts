import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    margin: 8,
    flex: 1,
  },
  title: {
    color: '#090909',
    fontSize: 32,
    fontWeight: 'bold',
  },
});
