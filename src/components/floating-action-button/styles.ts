import {StyleSheet} from 'react-native';

export const floatingActionsStyles = StyleSheet.create({
  button: {
    backgroundColor: '#000',
    borderRadius: 32,
    width: 64,
    height: 64,
    justifyContent: 'center',
    alignItems: 'center',
    position: 'absolute',
    right: 16,
    bottom: 16,
  },
});
