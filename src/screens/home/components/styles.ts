import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  userContainer: {
    padding: 8,
    borderColor: '#000',
    borderRadius: 16,
    borderWidth: 1,
    marginBottom: 16,
  },
  mainText: {
    fontWeight: 'bold',
  },
});
